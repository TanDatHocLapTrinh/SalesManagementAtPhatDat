<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!--Font Label Information-->
    <Style x:Key="FontLabelInfomationStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontFamily" Value="Montserrat"/>
        <Setter Property="Margin" Value="30,10,0,10"/>

    </Style>
    <!--Button Save Styles-->
    <Style x:Key="ButtonSaveStyle" TargetType="Button">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="#00CCFF"/>
        <Setter Property="Width" Value="100"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="Margin" Value="0,30,0,0"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="FontFamily" Value="Montserrat"/>
        <Setter Property="FontWeight" Value="DemiBold"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border CornerRadius="10"
                                    Background="{TemplateBinding Background}">
                        <ContentPresenter
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Content="{TemplateBinding Content}"
                                    TextBlock.Foreground="{TemplateBinding Foreground}"
                            />
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10"
                                                      ShadowDepth="3"
                                                      Opacity="0.2"
                                                      Color="Black"/>
                        </Border.Effect>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#66CCCC"/>
                <Setter Property="Foreground" Value="Black"/>
                <Setter Property="Width" Value="110"/>
                <Setter Property="Height" Value="45"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    <!--Text Box Infomation-->
    <Style x:Key="TextBoxAddStaffStyle" TargetType="TextBox">
        <Setter Property="Width" Value="700" />
        <Setter Property="Height" Value="30" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Padding" Value="3"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border BorderBrush="#66CCCC"
                            BorderThickness="1"
                            CornerRadius="5"
                            >
                        <ScrollViewer x:Name="PART_ContentHost"/>
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10"
                                              ShadowDepth="3"
                                              Opacity="0.2"
                                              Color="Black"/>
                        </Border.Effect>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border BorderBrush="#00CCFF"
                            BorderThickness="1"
                            CornerRadius="5"
                            >
                                <ScrollViewer x:Name="PART_ContentHost"/>
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="10"
                                              ShadowDepth="4"
                                              Opacity="0.5"
                                              Color="#00CCFF"/>
                                </Border.Effect>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>